apiVersion: v1
kind: Pod
metadata:
  name: app1
  namespace: default
  labels:
    app: app1
spec:
  containers:
  - name: app1
    image: "nginx:latest"
    resources:
      limits:
        cpu: 100m
        memory: 200Mi
    ports:
    - containerPort:  80
      name:  http
  restartPolicy: Always

---

apiVersion: v1
kind: Service
metadata:
  name: app1-service
  namespace: default
spec:
  selector:
    app: app1
  type: ClusterIP
  ports:
  - name: app1
    protocol: TCP
    port: 80
    targetPort: 80
